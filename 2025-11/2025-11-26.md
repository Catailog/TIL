# 2025-11-26 TIL

## 오늘 배운 것

- html, css 실습(landing page)
- Figma에 공유된 landing page를 구현해보았다.
- 스크롤 애니메이션을 위한 animation-timeline: view()를 사용해보았다. view의 인자로는 (axis view-timeline-inset) 값을 차례로 넣으면 된다. viewport의 위쪽에서 10% 안에 들어온 요소에 대해, viewport의 아래쪽에서 10%의 위치까지 애니메이션을 적용하고 싶다면 view(y 10% 10%)라고 적으면 된다. 만약 viewport 범위를 벗어난 고정된 범위에 대해 적용하고 싶다면 view(y)를 적고 animation-range를 적용해주면 된다. (예: animation-range: 1000px)
