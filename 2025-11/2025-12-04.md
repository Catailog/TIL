# 2025-12-04 TIL

## 오늘 배운 것

- 자바스크립트 구조 분해, this, map, set, json
- 중첩 객체의 구조 분해 시 null, undefined 값에 유의해야 한다. 구조 분해 중에 null 값에 .key 또는 undefined 값에 .key의 형태로 접근을 시도하여 대입하려는 경우가 발생할 수 있기 때문이다.
- 구조 분해 대입 시 대입하지 않고 건너뛰고 싶은 값이 있다면 아무것도 적지 않고 콤마를 찍어 넘길 수 있다. 이때 언더스코어로 명시해주면 좋을 거라고 생각했다. 그런데 const [_, _, thirdNum, _, fifthNum] = numbers;라고 적었더니 재선언 했다는 오류가 발생했다. 알고 보니 언더스코어는 ‘스킵 문법’ 같은 게 아니라 관례적으로 사용하던 변수명이었던 것이다.
