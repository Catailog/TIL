# 2025-12-16 TIL

## 오늘 배운 것

- 자바스크립트 서버 통신 연습
- 데이터를 보낼 때 보내는 쪽에서도 헤더가 필요하고 받는 쪽에서도 해당 데이터를 파싱할 준비가 되어있어야만 한다. 내 코드의 경우 클라이언트에선 `xhr.setRequestHeader('Content-Type', 'application/json');`으로 헤더를 설정해주고 서버에선 `app.use(express.json());`으로 미들웨어를 등록해줘야 정상적으로 데이터를 주고받을 수 있다.
